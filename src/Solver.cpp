// src/Solver.cpp
// NOTE:
// В этой версии проекта вся логика шага решателя перенесена в main.cpp (CPU-реализация)
// и в OpenCLSolver (OpenCL). Этот файл оставлен в виде пустой заглушки, чтобы
// сборка (cmake file(GLOB ... src/*.cpp)) не включала старую реализацию.
// Если вы предпочитаете — можете просто удалить этот файл.

#include "Types.hpp"

// ничего не реализуем здесь
